import styled from 'styled-components';

export const OpenBtn = styled.button(() => ({
  position: 'absolute',
  top: '20px',
  right: '20px',

  width: '40px',
  height: '40px',

  borderRadius: '30px',
}));
